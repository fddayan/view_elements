require "spec_helper"

describe ViewElements do
  it "has a version number" do
    expect(ViewElements::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
